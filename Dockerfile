# Використовуємо офіційний базовий образ Python версії 3.9
FROM python:3.10.7

# Задаємо робочу директорію /app у контейнері
WORKDIR /app

# Копіюємо файли проекту в поточну директорію контейнера (/app)
COPY . /app

# Встановлюємо залежності, вказані у файлі requirements.txt
RUN pip install -r requirements.txt

# Задаємо команду для запуску вашого застосунку
CMD ["python", "botweb1.py"]

